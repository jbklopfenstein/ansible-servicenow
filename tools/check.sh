#!/usr/bin/env bash

# Add your linter specific validation  of your code. This includes running tests, running linter jobs
# Heighliner executes checks.sh as a check job on Jenkins Server on every commit, before every build